version: v1
kind: Potential
spec:
  pair:
    type: stillinger-weber
    parameters:
      # Based on:
      # epsilon, sigma, a, lambda, gamma, cosheta0, A, B, p, q, tol
      - elements: ['O']
        coefficients: []
      # Guesses
      - elements: ['Li']
        coefficients: []
      - elements: ['Ta']
        coefficients: []
